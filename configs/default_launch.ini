; Hybrid storage configuration (HDF5 + SQLite)

[logging]
version = 1
disable_existing_loggers = false

[logging.formatters.standard]
format = "%(asctime)s - %(name)s - %(levelname)s - %(message)s"
datefmt = "%Y-%m-%d %H:%M:%S"

[logging.handlers.console]
class = logging.StreamHandler
level = INFO
formatter = standard
stream = ext://sys.stdout

[logging.handlers.file]
class = logging.handlers.RotatingFileHandler
level = DEBUG
formatter = standard
filename = "diffract.log"
maxBytes = 1048576
backupCount = 1

[logging.loggers.diffract]
level = DEBUG
handlers = ["console", "file"]
propagate = false

[logging.root]
level = DEBUG
handlers = ["console", "file"]

[storage]
backend = "hybrid"

[storage.hdf5]
path = "data/hybrid/hdf5_storage.h5"

[storage.sqlite]
path = "data/hybrid/sqlite_storage.db"

[metadata]
backend = "sqlite"

[metadata.sqlite]
path = "data/hybrid/metadata_index.db"

[cache]
backend = "redis"

[cache.redis]
max_memory_mb = 4096

[parallel.thread_pool]
max_workers = 8

[parallel.process_pool]
max_workers = 8

[nn.extractor]
skip_not_implemented_types = true

[export]
default_export_format = "pandas"
