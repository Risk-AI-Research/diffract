# Metric catalog

Every field the compute layer can produce, projected directly from the kernel
registry: the producing kernel, its display formula, apply level, required input
fields, and configurable parameters. See the per-category pages for
derivations, conventions, and interpretation.

```{include} _generated_table.md
```
